{
  "name": "ai-voice-agent-platform",
  "version": "1.0.0",
  "description": "AI Voice Agent Platform - Frontend and Backend",
  "scripts": {
    "dev": "concurrently --kill-others --prefix-colors \"cyan,magenta\" --prefix \"[{name}]\" \"npm:dev:frontend\" \"npm:dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && set PYTHONPATH=. && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "npm run dev",
    "install:all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["ai", "voice", "agent", "react", "fastapi"],
  "author": "",
  "license": "MIT"
}
